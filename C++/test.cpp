#include <iostream>
using namespace std;
int main(){
    system("python3 --version");
    //The system() function passes the given string to the CL command processor for processing
    //For example for the above it will display the Version of the python installed in the machine
    
    return 0;
}