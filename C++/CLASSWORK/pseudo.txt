//NUMBER GUESSING GAME
//GENERATE RANDOM NUMBERS BETWEEN 1 AND 10 ASK USER TO GUESS TILL THEY GET IT RIGHT

START
    SET RANDOM_NUMBER TO RANDINT(1,10)
    SET RUNNING TO TRUE
    WHILE RUNNING LOOP
    DISPLAY "ENTER A NUMBER TO GUESS"
    INPUT NUMBER
        IF NUMBER = RANDOM_NUMBER THEN
            DISPLAY "HOORAY YOU WIN"
            DISPLAY "GAME END"
            SET RUNNING TO FALSE
        END IF
        ELSE
            DISPLAY "WRONG TRY AGAIN"
        END IF
    END LOOP
END

QUESTION 4
//FIND THE MAXIMUM OF THREE NUMBERS
START
SET NUM1 TO 3
SET NUM2 TO 4
SET NUM3 TO 5
SET MAX TO NUM1
IF NUM2>MAX THEN
    SET MAX TO NUM2
END IF
IF NUM3>MAX THEN
    SET MAX TO NUM3
    END IF
END IF
DISPLAY MAX
END

//PASSWORD VALIDATOR
ASK USER FOR PASSWORD IF IT MATCHES PREDEIFNED PASSWORD DISPLAY "Acess Granted" Otherwise "Acess Denied"
START
    SET CORRECT_PASSWORD TO "1234"
    DISPLAY "ENTER YOUR PASSWORD"
    INPUT PASSWORD
    IF PASSWORD = CORRECT_PASSWORD THEN
        DISPLAY "Access Granted"
    ELSE
        DISPLAY "Access Denied"
    END IF
END

//MULTIPLICATION TABLE
Problem Ask the user for a number and print its multiplication table from 1 to 10
START
DISPLAY "Enter a number"
INPUT NUMBER
FOR I IN RANGE 1 TO 10
    DISPLAY NUMBER +"X" + I + NUMBER*I
END LOOP
END
🔹 7. Sum of Digits
Problem: Ask the user to enter a number. Display the sum of its digits.
enter a number eg 28 then add 2 + 8 you get 10 then display 10. this would need to separate then number 28 to 2 and 8
START
  DISPLAY "ENTER A NUMBER"
  INPUT NUMBER
  SET SIZE TO NUMBER.SIZE
  SET NUM_SUM TO 0
  FOR I IN RANGE 1 TO NUMBER.SIZE
    NUM_SUM +=I
  END LOOP
  DISPLAY NUM_SUM
END
    
  
END
